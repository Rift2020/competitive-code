/**
 *    author:  Rift
 *    created: 2022.08.11  10:57
**/
#include<bits/stdc++.h>
#define rep(i, a, b) for (int i = (a); i <= (b); ++i)
#define per(i, a, b) for (int i = (a); i >= (b); --i)
#define x first
#define y second
using namespace std;
using ll = long long;
using pr = pair<int,int>;

signed main(){
	ios::sync_with_stdio(false),cin.tie(nullptr);
	cout<<"hello"<<endl;	
	
	
	return 0;
}
