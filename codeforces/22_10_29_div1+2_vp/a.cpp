/**
 *    author:  Rift
 *    created: 2022.10.30  14:19
**/
#include<bits/stdc++.h>
#define rep(i, a, b) for (int i = (a); i <= (b); ++i)
#define per(i, a, b) for (int i = (a); i >= (b); --i)
#define x first
#define y second
using namespace std;
using ll = long long;
using pr = pair<int,int>;
int t;
signed main(){
	ios::sync_with_stdio(false),cin.tie(nullptr);
	cin>>t;
	while(t--){
		int in;
		cin>>in;
		cout<<in<<endl;
	}
	
	
	return 0;
}
